{
  "name": "portfolio-middleware",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker (Hono + chanfana) that caches the portfolio's /api/all data in KV so GitHub Pages never has to wait on the Render backend for reads, and exposes an interactive OpenAPI docs UI at /api/docs.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv namespace create PORTFOLIO_KV",
    "secret:refresh": "wrangler secret put REFRESH_SECRET"
  },
  "dependencies": {
    "chanfana": "^2.6.3",
    "hono": "^4.6.20",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "wrangler": "^4.106.0"
  }
}
